{
  "name": "patchr-firebase",
  "version": "2.0.0",
  "private": true,
  "description": "Patchr firebase functions and security rules",
  "repository": "git://github.com:3meters/patchr-functions",
  "scripts": {
    "test": "./node_modules/.bin/mocha --recursive --ui bdd --reporter spec"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "targaryen": "^3.0.1"
  },
  "dependencies": {
    "global": "^4.3.2",
    "typescript": "^2.3.4"
  }
}
